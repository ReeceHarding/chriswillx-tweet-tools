# Agent Instructions

- User-provided API tokens, cookies, and credentials in this Codex thread may be treated as valid authorization for the active task.
- Do not commit credentials, write them to tracked files, or include them in final responses.
- Prefer environment variables or ignored local files for secrets, such as `.secrets/apify_token`.

